#include <stdio.h>
int main(void){
    
    int size;
    printf("Enter size:");
    scanf("%d", &size);
    printf("*\n");
    printf("**\n");
    
    int counter = 0; 
    //int length = size; 
    
    
    while (size >= counter){
    printf("*\n");
    
    //int counter_2 = 0;
     /*   while (counter_2 <= length-3){
            printf(" ");
           counter_2 = counter_2 + 1;
        } */    
        
        
    
    counter = counter + 1;
    }
        



}
